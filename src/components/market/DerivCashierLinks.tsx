import { ArrowDownToLine, ArrowUpFromLine, ExternalLink } from 'lucide-react'

export function DerivCashierLinks() {
  const openCashier = () => window.open('https://app.deriv.com/cashier', '_blank', 'noopener,noreferrer')
  return <div className="rounded-lg border border-shafx-border bg-shafx-bg p-3"><div className="text-[10px] uppercase tracking-wider text-shafx-textMuted">Broker funds</div><div className="mt-1 text-sm font-semibold">Deposit or withdraw with Deriv</div><p className="mt-1 text-[10px] leading-relaxed text-shafx-textMuted">SHAFX does not hold your money. Use Deriv's official Cashier, then return to SHAFX and refresh your broker balance.</p><div className="mt-2 grid grid-cols-2 gap-2"><button type="button" onClick={openCashier} className="flex min-h-10 items-center justify-center gap-1 rounded border border-shafx-border text-[10px] font-semibold"><ArrowDownToLine className="h-3.5 w-3.5" />Deposit</button><button type="button" onClick={openCashier} className="flex min-h-10 items-center justify-center gap-1 rounded border border-shafx-border text-[10px] font-semibold"><ArrowUpFromLine className="h-3.5 w-3.5" />Withdraw</button></div><div className="mt-2 flex items-center gap-1 text-[9px] text-shafx-textMuted"><ExternalLink className="h-3 w-3" />Opens Deriv's official cashier</div></div>
}
